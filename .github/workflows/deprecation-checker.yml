name: Monthly Deprecation Check
run-name: Checking for Deprecation Warnings

on:
  # Run on the 1st day of every month at 9:00 AM UTC
  schedule:
    - cron: '0 9 1 * *'
  # Allow manual trigger for testing
  workflow_dispatch:

jobs:
  check-deprecations:
    name: Check for Deprecation Warnings
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read

    steps:
      - name: Fetch Sources
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Setup Gradle Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-${{ hashFiles('**/*.gradle*') }}-${{ hashFiles('**/gradle/wrapper/gradle-wrapper.properties') }}

      - name: Build and Check for Deprecations
        id: build
        continue-on-error: true
        run: |
          # Create output directory
          mkdir -p build-reports
          
          # Run build with warnings enabled and capture output
          # Using --warning-mode=all to show all warnings including deprecations
          ./gradlew build --warning-mode=all --stacktrace 2>&1 | tee build-reports/build-output.log
          
          # Extract deprecation warnings from build output
          grep -i "deprecat" build-reports/build-output.log > build-reports/deprecations.txt || true
          grep -i "warning.*has been deprecated" build-reports/build-output.log >> build-reports/deprecations.txt || true
          
          # Also check for Kotlin deprecation warnings
          grep -i "warning.*@deprecated" build-reports/build-output.log >> build-reports/deprecations.txt || true
          
          # Filter out known AGP-internal deprecation warnings that cannot be fixed in this project
          # These are multi-string notation warnings from inside AGP itself (not our code)
          # Only filters these specific patterns to avoid silencing legitimate warnings:
          # - com.android.tools.lint:lint-gradle (AGP internal dependency)
          # - com.android.tools.build:aapt2 (AGP internal dependency)
          if [ -f build-reports/deprecations.txt ] && [ -s build-reports/deprecations.txt ]; then
            grep -v 'com\.android\.tools\.lint:lint-gradle' build-reports/deprecations.txt | \
              grep -v 'com\.android\.tools\.build:aapt2' > build-reports/deprecations-filtered.txt || true
            mv build-reports/deprecations-filtered.txt build-reports/deprecations.txt
          fi
          
          # Remove duplicates and sort
          sort -u build-reports/deprecations.txt -o build-reports/deprecations.txt
          
          # Simplify file paths - remove absolute path prefix and group by file
          if [ -f build-reports/deprecations.txt ] && [ -s build-reports/deprecations.txt ]; then
            # Get the workspace directory
            WORKSPACE_DIR=$(pwd)
            # Replace absolute paths with relative paths from workspace root
            sed -i.bak "s|$WORKSPACE_DIR/||g" build-reports/deprecations.txt
            rm -f build-reports/deprecations.txt.bak
          fi
          
          # Count deprecation warnings
          DEPRECATION_COUNT=$(wc -l < build-reports/deprecations.txt | tr -d ' ')
          echo "Found $DEPRECATION_COUNT deprecation warnings"
          echo "deprecation_count=$DEPRECATION_COUNT" >> $GITHUB_OUTPUT

      - name: Run Dependency Update Check
        id: dependency-updates
        continue-on-error: true
        run: |
          # Check for outdated dependencies that might have deprecations
          ./gradlew dependencyUpdates -Drevision=release 2>&1 | tee build-reports/dependency-updates.log
          
          # Extract outdated dependencies - capture everything from "The following dependencies have later" 
          # until the next section or end of relevant output
          sed -n '/The following dependencies have later/,/^$/p' build-reports/dependency-updates.log > build-reports/outdated-deps.txt || true
          
          # Filter out known transitive dependencies that are managed by plugins (not direct dependencies)
          # These updates cannot be controlled in this project and will be updated when the parent plugin updates
          # Only filters these specific packages to avoid silencing legitimate dependency updates:
          # - com.pinterest.ktlint:* (transitive from ktlint-gradle plugin)
          # - io.github.detekt.sarif4k:* (transitive from ktlint or detekt)
          # - io.github.oshai:kotlin-logging (transitive from ktlint)
          if [ -f build-reports/outdated-deps.txt ] && [ -s build-reports/outdated-deps.txt ]; then
            # Use awk to filter out both the dependency line and its following URL line
            awk '
              /^ - com\.pinterest\.ktlint:ktlint-cli / { skip=1; next }
              /^ - com\.pinterest\.ktlint:ktlint-cli-reporter-baseline / { skip=1; next }
              /^ - com\.pinterest\.ktlint:ktlint-ruleset-standard / { skip=1; next }
              /^ - io\.github\.detekt\.sarif4k:sarif4k / { skip=1; next }
              /^ - io\.github\.oshai:kotlin-logging / { skip=1; next }
              skip && /^     https:/ { skip=0; next }
              { skip=0; print }π
            ' build-reports/outdated-deps.txt > build-reports/outdated-deps-filtered.txt || true
            mv build-reports/outdated-deps-filtered.txt build-reports/outdated-deps.txt
          fi

      - name: Lint Check for Deprecations
        id: lint
        continue-on-error: true
        run: |
          # Run lint to detect deprecated API usage (Android specific)
          ./gradlew lintDebug || true
          
          # Find and collect lint reports
          find . -name "lint-results-debug.xml" -o -name "lint-results-debug.txt" | while read file; do
            if [ -f "$file" ]; then
              echo "Found lint report: $file"
              # Extract deprecation-related issues
              grep -i "deprecat" "$file" >> build-reports/lint-deprecations.txt || true
            fi
          done
          
          # Count lint deprecations
          if [ -f build-reports/lint-deprecations.txt ]; then
            LINT_DEPRECATION_COUNT=$(wc -l < build-reports/lint-deprecations.txt | tr -d ' ')
            echo "Found $LINT_DEPRECATION_COUNT lint deprecation warnings"
            echo "lint_deprecation_count=$LINT_DEPRECATION_COUNT" >> $GITHUB_OUTPUT
          else
            echo "lint_deprecation_count=0" >> $GITHUB_OUTPUT
          fi

      - name: Generate Report Summary
        id: summary
        run: |
          # Create a summary report
          REPORT_FILE="build-reports/deprecation-report.md"
          
          # Get counts from previous step outputs
          BUILD_COUNT="${{ steps.build.outputs.deprecation_count }}"
          LINT_COUNT="${{ steps.lint.outputs.lint_deprecation_count }}"
          
          # Calculate total (default to 0 if empty)
          TOTAL_COUNT=$((${BUILD_COUNT:-0} + ${LINT_COUNT:-0}))
          
          echo "# Deprecation Report - $(date +'%Y-%m-%d')" > $REPORT_FILE
          echo "" >> $REPORT_FILE
          echo "**Total deprecations found: $TOTAL_COUNT**" >> $REPORT_FILE
          echo "" >> $REPORT_FILE
          
          echo "total_count=$TOTAL_COUNT" >> $GITHUB_OUTPUT
          
          if [ $TOTAL_COUNT -gt 0 ]; then
            echo "has_deprecations=true" >> $GITHUB_OUTPUT
          
            # Add artifact download links
            echo "## 📦 Download Detailed Reports" >> $REPORT_FILE
            echo "" >> $REPORT_FILE
            echo "Download the complete build artifacts for detailed analysis:" >> $REPORT_FILE
            echo "" >> $REPORT_FILE
            echo "🔗 **[Download All Reports (deprecation-reports-${{ github.run_number }})](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})**" >> $REPORT_FILE
            echo "" >> $REPORT_FILE
            echo "The artifact includes:" >> $REPORT_FILE
          
            if [ -f build-reports/deprecations.txt ] && [ -s build-reports/deprecations.txt ]; then
              echo "- \`deprecations.txt\` - Build deprecation warnings ($BUILD_COUNT)" >> $REPORT_FILE
            fi
          
            if [ -f build-reports/lint-deprecations.txt ] && [ -s build-reports/lint-deprecations.txt ]; then
              echo "- \`lint-deprecations.txt\` - Lint deprecation warnings ($LINT_COUNT)" >> $REPORT_FILE
            fi
          
            if [ -f build-reports/outdated-deps.txt ] && [ -s build-reports/outdated-deps.txt ]; then
              echo "- \`outdated-deps.txt\` - Outdated dependencies" >> $REPORT_FILE
            fi
          
            echo "- \`build-output.log\` - Complete build output" >> $REPORT_FILE
            echo "" >> $REPORT_FILE
            echo "---" >> $REPORT_FILE
            echo "" >> $REPORT_FILE
          
            # Build Deprecation Warnings grouped by file
            if [ -f build-reports/deprecations.txt ] && [ -s build-reports/deprecations.txt ]; then
              echo "## Build Deprecation Warnings ($BUILD_COUNT)" >> $REPORT_FILE
              echo "" >> $REPORT_FILE
          
              # Process and group deprecations by file
              # Format is typically: "w: file:///path/to/file.kt:42:5: warning message"
              # or "path/to/file.kt:42: warning message"
              awk '
              {
                line = $0
          
                # Remove "w: file://" prefix if present
                gsub(/^w: file:\/\//, "", line)
          
                # Extract file path (everything before the line number)
                # Match pattern: filepath:number: or filepath:number:number:
                if (match(line, /^([^:]+):([0-9]+)/, arr)) {
                  file = arr[1]
                  # Get the rest after file:line
                  rest = substr(line, length(arr[0]) + 1)
          
                  # Print file header if changed
                  if (file != prev_file) {
                    if (prev_file != "") print ""
                    print "### `" file "`"
                    print ""
                    prev_file = file
                  }
          
                  # Print the warning (line number and message)
                  gsub(/^:/, "", rest)  # Remove leading colon
                  print "- Line " arr[2] rest
                } else {
                  # Fallback: just print the line if pattern doesn match
                  print "- " line
                }
              }' build-reports/deprecations.txt >> $REPORT_FILE
              echo "" >> $REPORT_FILE
            fi
          
            # Lint Deprecation Warnings
            if [ -f build-reports/lint-deprecations.txt ] && [ -s build-reports/lint-deprecations.txt ]; then
              echo "## Lint Deprecation Warnings ($LINT_COUNT)" >> $REPORT_FILE
              echo "" >> $REPORT_FILE
              echo '```' >> $REPORT_FILE
              head -100 build-reports/lint-deprecations.txt >> $REPORT_FILE
              echo '```' >> $REPORT_FILE
              echo "" >> $REPORT_FILE
            fi
          
            # Outdated Dependencies
            if [ -f build-reports/outdated-deps.txt ] && [ -s build-reports/outdated-deps.txt ]; then
              echo "## Outdated Dependencies" >> $REPORT_FILE
              echo "" >> $REPORT_FILE
              echo '```' >> $REPORT_FILE
              cat build-reports/outdated-deps.txt >> $REPORT_FILE
              echo '```' >> $REPORT_FILE
              echo "" >> $REPORT_FILE
            fi
          else
            echo "has_deprecations=false" >> $GITHUB_OUTPUT
            echo "✅ **No deprecation warnings found!**" >> $REPORT_FILE
          fi
          
          # Store report content for issue creation
          echo "REPORT_CONTENT<<EOF" >> $GITHUB_ENV
          cat $REPORT_FILE >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Create GitHub Issue
        if: steps.summary.outputs.has_deprecations == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const reportContent = process.env.REPORT_CONTENT;
            const totalCount = '${{ steps.summary.outputs.total_count }}';
            const issueTitle = `[Deprecation Check] ${totalCount} deprecation warning(s) detected`;
            const currentDate = new Date().toISOString().split('T')[0];
            
            // Check if there's already an open issue for deprecations
            const existingIssues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'deprecation-warning',
              per_page: 100
            });
            
            // Find existing open deprecation check issue
            const existingIssue = existingIssues.data.find(issue => 
              issue.title.includes('[Deprecation Check]')
            );
            
            // Create new issue
            const newIssue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: issueTitle,
              body: reportContent,
              labels: ['deprecation-warning', 'technical-debt']
            });
            
            core.info(`Created new issue #${newIssue.data.number}`);
            
            // If there was an existing issue, close it with a reference to the new one
            if (existingIssue) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existingIssue.number,
                body: `This issue has been superseded by #${newIssue.data.number} from the latest deprecation check on ${currentDate}.`
              });
            
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existingIssue.number,
                state: 'closed'
              });
            
              core.info(`Closed previous issue #${existingIssue.number}`);
            }

      - name: Upload Deprecation Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deprecation-reports-${{ github.run_number }}
          path: build-reports/
          retention-days: 90

      - name: Workflow Summary
        if: always()
        run: |
          echo "## Deprecation Check Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ steps.summary.outputs.has_deprecations }}" == "true" ]; then
            echo "⚠️ **Found ${{ steps.summary.outputs.total_count }} deprecation warning(s)**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "A GitHub issue has been created/updated with the details." >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📦 Download Reports" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🔗 [Download deprecation-reports-${{ github.run_number }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ **No deprecation warnings found!**" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Fail if Deprecations Found
        if: steps.summary.outputs.has_deprecations == 'true'
        run: |
          echo "::error::Found ${{ steps.summary.outputs.total_count }} deprecation warning(s). Check the created issue for details."
          exit 1
