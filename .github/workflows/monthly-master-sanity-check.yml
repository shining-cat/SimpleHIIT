name: Monthly Master Sanity Check
run-name: Running Monthly Master Sanity Check

on:
  schedule:
    - cron: '0 9 1 * *'
  workflow_dispatch:

jobs:
  verify-module-dependencies:
    name: Verify Module Dependencies
    uses: ./.github/workflows/module-dependencies-checker.yml
    permissions:
      issues: write
      contents: read

  verify-dependency-graph:
    name: Verify Dependency Graph
    uses: ./.github/workflows/dependency-graph-checker.yml
    permissions:
      issues: write
      contents: read

  check-deprecations:
    name: Check Deprecations
    uses: ./.github/workflows/deprecation-checker.yml
    permissions:
      issues: write
      contents: read

  check-dependency-updates:
    name: Check Dependency Updates
    uses: ./.github/workflows/dependency-updates-checker.yml
    permissions:
      issues: write
      contents: read
